# ============================================================
# SR_GAME_CHINESE — 换行符与编码约定
#
# 原则：**本仓库不做任何换行符归一化。**
#
# 为什么：本项目源码来自两个既有仓库，其中大量文件（.h/.cpp/.py/.json/
#   .vcxproj）的原始内容就是 CRLF。强制 eol=lf 会改写这些文件的字节内容，
#   使新仓库与旧仓库产生 50+ 处内容差异 —— 迁移应当是无损的。
#
# 因此本文件只做两件事：
#   1) 声明二进制格式，避免 git 对它们做行尾/diff 处理；
#   2) 不做任何 eol 强制。
#
# 若将来要统一换行符，应当是一次独立的、显式的重构提交，
# 而不是混在仓库迁移里。
# ============================================================

# ---- 默认：自动识别文本，但不改换行符 ----
* text=auto

# ---- Windows 专用脚本（保持 CRLF 语义）----
*.bat           text eol=crlf
*.cmd           text eol=crlf

# ---- 文档与脚本统一纯 LF ----
# 本项目所有 .md / .py 均为手写、从未有过 CRLF 版本，强制 LF 不会产生内容差异；
# 而 dist/ 下的 le_strings 也依赖二进制语义，故此处显式声明。
*.md            text eol=lf
*.py            text eol=lf
*.le_strings    binary

# ---- 明确的二进制格式 ----
*.ttf           binary
*.otf           binary
*.dll           binary
*.asi           binary
*.exe           binary
*.lib           binary
*.pdb           binary
*.bin           binary
*.zip           binary
*.7z            binary
*.png           binary
*.jpg           binary
*.ico           binary

# ============================================================
# 归档区：按二进制处理，git 不碰换行符、不做 diff 显示
# （必须是文件最后一条规则：gitattributes 后匹配者胜出）
# ============================================================
/Archives/**    -text -diff
